#include <stdio.h>
#include <stdlib.h>

float division(){
    float a, b;
    printf("Type the first number: ");
    scanf("%f", &a);
    printf("Type the second number: ");
     scanf("%f", &b);
     float result = a / b;
     return result;
}

